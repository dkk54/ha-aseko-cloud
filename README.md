# Aseko Cloud — Home Assistant integration

Official Home Assistant integration for Aseko pool units (ASIN AQUA), built by
Aseko on top of the **public integrator API** at
[`https://api.aseko.cloud/api/v1`](https://api.aseko.cloud/api/v1/docs).

## What it does

- Signs in with an **Aseko API key** (Bearer token) — generated by the user at
  <https://account.aseko.cloud/profile/settings/api-keys>.
- Discovers every paired pool unit on the account; each becomes one device.
- **Sensors** (created only for fields the unit actually reports):
  - Temperatures (water, air, solar, required water temperature)
  - pH and required pH
  - Redox and required redox
  - Free chlorine, combined chlorine, required free chlorine
  - Salinity, electrode power
  - Filter flow, filter pressure
  - Water level
  - Enum states: mode, filtration speed, pool flow, water-level state,
    electrolyzer direction
- **Binary sensors**: connectivity, problem (any ERROR-severity status message,
  with the messages as attributes), filtration / heating / solar / electrolyzer
  running, water filling, water flow to probes.
- Polls every 5 minutes (`cloud_polling`). The integrator API is read-only.

## Architecture

```
custom_components/aseko_cloud/
  __init__.py        Config entry setup
  manifest.json      Integration metadata (cloud_polling)
  const.py           Constants and API URLs
  api.py             Aseko integrator API client + data model (no HA imports)
  coordinator.py     DataUpdateCoordinator (5-minute poll)
  config_flow.py     API-key entry + reauthentication
  entity.py          Base entity (one device per pool unit)
  sensor.py          Sensors driven by statusValues fields
  binary_sensor.py   Connectivity / problem / running states
  strings.json       Translation source
  translations/      en, cs
  icons.json         Entity icons
hacs.json            HACS metadata
```

## Installation

### Via HACS (recommended)

[![Open your Home Assistant instance and open a repository inside the Home Assistant Community Store.](https://my.home-assistant.io/badges/hacs_repository.svg)](https://my.home-assistant.io/redirect/hacs_repository/?owner=dkk54&repository=ha-aseko-cloud&category=integration)

> **Aseko Cloud is not in the HACS default store yet**
> ([pending review](https://github.com/hacs/default/pull/7991)), so for now you
> add it as a **custom repository**. The one-click badge above does this for you;
> the manual steps are below. Once the review merges, you'll be able to find it
> directly in HACS without step 2.

**Prerequisite:** [HACS](https://hacs.xyz) must already be installed.

1. In Home Assistant, open **HACS** in the sidebar.
2. **Add the custom repository:** click the **⋮** menu (top-right) →
   **Custom repositories**, then enter:
   - **Repository:** `https://github.com/dkk54/ha-aseko-cloud`
   - **Type / Category:** **Integration**

   Click **Add**. (Or just click the badge above, which fills this in.)
3. **Download:** search **Aseko Cloud** in HACS, open it, and click
   **Download**.
4. **Restart Home Assistant** — *required*. The integration only appears in the
   next step after a restart.
5. **Settings → Devices & Services → Add Integration**, search **Aseko Cloud**
   and select it. It carries the *custom integration* badge and asks for an
   **API key** — not "Aseko Pool" (the separate core integration, which asks for
   email/password).
6. Paste an API key generated at
   <https://account.aseko.cloud/profile/settings/api-keys>.

> **"Aseko Cloud" doesn't show up in step 5?** Make sure you restarted (step 4).
> If it's still missing, it's a stale browser cache: hard-refresh the page
> (**Ctrl/Cmd + Shift + R**) or open Home Assistant in a private window, then
> search again.

### Manual

Copy `custom_components/aseko_cloud/` into your Home Assistant
`config/custom_components/` directory and restart.

## Open items / roadmap

- [ ] End-to-end test with a real API key (the only thing left to verify).
- [ ] Expose `upcomingFiltrationPeriod` (start / end / non-stop) as a sensor.
- [ ] Config-entry diagnostics + test suite.
- [ ] Push support: the integrator API is poll-only today. Adding webhooks or
      SSE to the integrator API would let this integration become `cloud_push`
      and remove the 5-minute lag.
- [ ] Control endpoints (set required pH / chlorine, switch mode, …): the
      integrator API is read-only today; adding write endpoints would let the
      integration expose `number`/`select`/`switch` platforms.
- [ ] Submit to Home Assistant core to supersede the third-party
      `aseko_pool_live`. For that the `api.py` client must move to a separate
      PyPI package and be referenced from `manifest.json` `requirements`.

## Notes for Aseko

The integration sends the following headers on every request, as documented in
the integrator API spec:

| Header           | Value                          |
|------------------|--------------------------------|
| `Authorization`  | `Bearer <api key>`             |
| `X-Client-Name`  | `home_assistant`               |
| `X-Client-Version` | matches `manifest.json` `version` |
| `Accept-Language`| Home Assistant's configured language |

Two small server-side improvements would let the integration be polished:

1. **Return an account identifier** somewhere (e.g. `/auth/check`) so the
   config entry can use it as `unique_id` instead of hashing the API key.
2. **Include `firmware`** on `PairedUnit` so the device's `sw_version` can be
   set in Home Assistant.

## License

Apache-2.0 — see [`LICENSE`](LICENSE).
